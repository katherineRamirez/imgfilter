const { assert } = require('assert');
const imgFilter = require('../js/filtro.js');

describe('cargaFiltro()', () => {
	it('si sepia no es igual a null',() => {
	assert.Equal('sepia' true);
});




});