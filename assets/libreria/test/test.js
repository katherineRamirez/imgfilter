const { assert } = require('chai');
const imgFilter = require('../js/filtro.js');

describe('', function(){
	it('shoul add the same name', function(){
	assert.equal('filter: sepia(85%)', false);
});



	it('shoul add the same name', function(){
	assert.equal('filter: grayscale(100%)', true);
});



	it('shoul add the same name', function(){
	assert.equal('filter: saturation(180%)', true);

});

});

